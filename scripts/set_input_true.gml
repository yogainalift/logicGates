///argument is the input number you want
if(instance_find(obj_input,argument0-1)){
   with(instance_find(obj_input,argument0-1)){
        output = true;
        image_index = 0;
        image_blend = make_colour_hsv(80, 240, 250);
        enable_wire(wire);
   }
}
